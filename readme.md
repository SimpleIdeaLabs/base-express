# base-express

- boilerplate for express backend

## getting started

- `cp example.env .env`
- `yarn`
- `yarn run build`

## running with docker

- `docker-compose up`
- `Update .env file accordingly`

## accessing api

- access `http://localhost:3001/test` to verify
